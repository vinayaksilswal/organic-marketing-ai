# Dev/test dependencies — install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.5
pytest-asyncio==0.25.3
aiosqlite==0.21.0
pglast>=7.0
# Catches names used but never imported — twice the cause of a production
# 500 that both an import check and a green suite let through.
pyflakes>=3.2
