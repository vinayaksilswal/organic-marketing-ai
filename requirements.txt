# =============================================================================
# Organic Marketing AI — Python Backend Dependencies (Production-Ready)
# =============================================================================

# --- Web Framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.3
gunicorn==23.0.0

# --- Templating & Static Files ---
jinja2==3.1.6
python-multipart==0.0.20

# --- Database ORM ---
sqlalchemy[asyncio]>=2.0.0
asyncpg==0.30.0

# --- Configuration ---
pydantic==2.11.5
pydantic-settings==2.9.1
python-dotenv==1.1.0

# --- HTTP Client (async) ---
httpx==0.28.1

# --- Retry & Resilience ---
tenacity==9.1.2

# --- Task Scheduling ---
apscheduler==3.11.0
arq==0.26.1
redis==5.3.0

# --- Cloud Media Storage (Cloudinary) ---
cloudinary==1.43.0

# --- Email (Resend SDK) ---
resend==2.7.0

# --- Twitter/X API ---
tweepy==4.15.0

# --- Authentication & Encryption ---
pyjwt==2.9.0
bcrypt==4.3.0
cryptography>=41.0.0

# --- Logging ---
loguru==0.7.3

# --- Database Migrations ---
alembic==1.15.2

# --- Monitoring ---
prometheus-fastapi-instrumentator==7.0.2

# --- Monetization & Security ---
slowapi==0.1.9

# --- Error Tracking ---
sentry-sdk[fastapi]==2.22.0

# --- Testing (dev) ---
# pytest==8.3.5
# pytest-asyncio==0.25.3
# httpx  # already installed

# Video post-processing: a branded end card composited onto each clip before
# posting. A diffusion model cannot render a legible sentence or URL — ffmpeg
# draws real text, identically every time. imageio-ffmpeg ships a static
# binary so the container does not depend on a system ffmpeg being present.
imageio-ffmpeg==0.6.0
Pillow==12.3.0

# Imported at module level by services/catalog_service.py and
# services/rendering_service.py. It was never declared, so those modules only
# imported when something else happened to pull aiohttp in. When it was absent
# the import escaped as a 500 from workspace creation.
aiohttp==3.11.11
